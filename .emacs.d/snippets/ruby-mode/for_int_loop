# -*- coding: utf-8; mode: snippet -*-
# name : NからMまでの繰り返し (<= for (i = N, i < M, i++) 相当)
# --
for ${1:i} in ${2:N}...${3:M}
  $0
end
# 備考: 以下のように書いても同様のことが可能
#       $2.upto($3-1) do |$1| ... end
# 備考: 逆に, 値を小さくしながらループしたい場合は, 以下のように書けばいい (for (i = M-1; i >= N; i--))
#       ($3-1).downto($2) do |$1| ... end
# 備考: より柔軟に書きたければ, 以下のように書けばいい (開始値, 終端値, ステップ幅, を指定)
#       START.step(END, STEP) do |$1| ... end
# -*- coding: utf-8; mode: snippet -*-
# name: nonlocal (変数スコープ(scope)の宣言)
# key: nonlocal
# --
nonlocal ${1:x, y}  # (Python 3 only)  $1を (ローカルな変数ではなく) outer scope の変数、と宣言 (最も近いのscopeの同名変数がbindされる)
# 注意: 参照するだけなら不要. 代入する場合に必要.
# 注意: 既に同名の変数がローカルに定義されてしまっていると使えない
# -*- coding: utf-8; mode: snippet -*-
# name: iterable-min (最小値, minimum)   [string, list, tuple, bytearray, buffer, xrange, set, dict(hash/map), file, iterator/generator, ]
# key: min
# --
min(${1:iterable})
# -*- coding: utf-8; mode: snippet -*-
# name: global (変数スコープ(scope)の宣言)
# key: global
# --
global ${1:x, y}  # $1を (ローカルな変数ではなく) global なスコープの変数、と宣言
# 注意: 参照するだけなら global は不要. 代入する場合に必要..
# 注意: この指示句の効果範囲は global 文と同時に読み込まれたコードのみ。
#       (例えば、exec 文内に入っている global 文は、その exec 文が属するスコープのコードとは無関係)
# -*- coding: utf-8; mode: snippet -*-
# name: dir (クラス/モジュールで定義されている名前一覧の確認, オブジェクトのプロパティ/メソッドの確認,  methods)
# key: dir
# --
dir(${1:sys})  # 引数がモジュールの場合は, モジュールで定義されている名前一覧
# dir(str)     # 引数がクラス/型の場合は, その属性+再帰的にたどった基底クラスの属性の一覧
# dir(42)      # 引数がオブジェクトの場合は, 属性名, クラス属性名, 再帰的にたどった基底クラスの属性名
# dir()        # 引数がなければ, 現在のローカルスコープに定義している名前の一覧
# dir(__builtin__)  # 組み込みの変数/関数一覧 (Python 2)
# dir(builtin)  # 組み込みの変数/関数一覧 (Python 3)

# 備考: オブジェクトが __dir__() をオーバーライドしていれば, その値が使われる.
#       そうでなければ, __dict__ 属性, そして型オブジェクトが使われる.
#       (この場合, カスタムの __getattr__() を持つオブジェクトでは不正確になるかもしれない)
# 備考: 主に対話プロンプトでの使用を想定し、厳密性や一貫性よりも、
#       興味を引くような名前のセットを返すことを重視する。
#       このため、細かい動作はリリース間で変わる可能性がある。
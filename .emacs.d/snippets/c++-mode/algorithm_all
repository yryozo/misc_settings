# -*- coding: utf-8; mode: snippet -*-
# name: algorithm_all?(all_of, every, for_all)
# key: algorithm_all?
# --
if (std::all_of( ${2:vv}.begin(), $2.end(), [${3:=}](decltype(*begin($2)) ${4:x}) -> bool {
      return ;$0 $4 > 42;
    })) {
  // 全要素が条件を満たした場合:
  
}
# -*- coding: utf-8; mode: snippet -*-
# name: algorithm_any?(any_of, exist)
# key: algorithm_any?
# --
if (std::any_of( ${2:vv}.begin(), $2.end(), [${3:=}](decltype(*begin($2)) ${4:x}) -> bool {
      return ;$0 $4 > 42;
   })) {
  // どれかの要素が条件を満たした場合:
  
}